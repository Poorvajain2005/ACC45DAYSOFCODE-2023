#include <stdio.h>

int main(void) {
    int t,n;
    scanf("%d",&t);
    while(t>0)
    {
        scanf("%d",&n);
        if(n%2==0||n==1)
        printf("Yes\n");
        else
        printf("No\n");
        t--;
    }
	return 0;
}

