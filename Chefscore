#include<stdio.h>
int main()
{
    int t,n,x,y;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d%d%d",&n,&x,&y);
        if(y%x==0)
        printf("YES\n");
        else
        printf("NO\n");
    }
}
