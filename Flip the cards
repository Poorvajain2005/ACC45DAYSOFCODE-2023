#include<iostream>
using namespace std;
int main()
{
	int t,x,y,end1;
	cin>>t;
	while(t--)
	{
		cin>>x>>y;
		if(x==1 or y==1)
		cout<<"No"<<end1;
		else{
			if(x%2==1 && y%2==1)
			cout<<"No"<<end1;
			else
			cout<<"Yes"<<end1;
		}
	}
	return 0;
}
