#include <stdio.h>

int main()
{
    int a,b,x,y,n,i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d%d%d%d",&a,&b,&x,&y);
        if(a+x>=b && a-y<=b){
            printf("YES\n");
        }
        else{
            printf("NO\n");
        }
	}return 0;
}
